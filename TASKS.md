# TASKS.md

## Current task

**T-005 — real browser/e2e verification of the live deployment. Status: not started.** Objective: run `TESTING.md`'s manual two-browser smoke-test checklist against https://pocket-party-eta.vercel.app, and run `npm run test:e2e` for the first time — both are unblocked (real credentials + a live deployment exist) but neither has actually been done. This is referenced consistently as the current task across `CLAUDE.md`, `PROJECT_STATE.md`, `TASKS.md`, and `HANDOFF.md`.

T-003 and T-004 (below) are both complete — the backend migration and the realtime rework are finished, kept here for historical record. T-001/T-002 (the old "connect Supabase" tasks) were superseded when the user stated directly, "use clerk and neon i cant use supabase." **T-003 (migrate off Supabase onto Clerk + Neon/Drizzle + PartyKit/Cloudflare) ran through all 5 phases.** Afterward, the user questioned whether Cloudflare's paid Durable Objects plan ($5/month) was actually needed for a casual friends-game ("do i need cloud flare... why dont i just use like a party code so we dont need all this stuff"), and after weighing options, chose Ably ("ok then ya ably and free"). **T-004 (replace PartyKit/Cloudflare Durable Objects with Ably) is now done** — see `DECISIONS.md` D-018 for the full reasoning.

**T-003 — full detail, for historical record (kept consistent across `CLAUDE.md`, `PROJECT_STATE.md`, `TASKS.md`, and `HANDOFF.md`):**

- **Exact objective:** replace Supabase (Auth + Postgres + Realtime) with Clerk (real accounts only) + Neon/Drizzle (database) + PartyKit on Cloudflare (realtime multiplayer sync), without changing any of the 10 games' engine/board logic, which is pure and framework-agnostic already.
- **Phases, all done:**
  1. **Neon + Drizzle schema** — ✅ `src/lib/db/schema.ts` (8 tables), `src/lib/db/client.ts`, `src/lib/db/env.ts`, `drizzle.config.ts`, generated migrations in `drizzle/`, seed script `src/lib/db/seed.ts` + `npm run db:seed`. **Not yet applied to a live database** — no `DATABASE_URL` supplied yet.
  2. **Identity layer** — ✅ `@clerk/nextjs` installed; `src/lib/identity/guest-cookie.ts` + `get-current-actor.ts`; `src/middleware.ts` runs Clerk + the guest-cookie bootstrap; `src/app/layout.tsx` wrapped in `<ClerkProvider>`; `account-linking.tsx` rebuilt around Clerk's modals. **Not yet verified against a real Clerk application.**
  3. **PartyKit room/match engine, including both Follow-ups** — ✅ `party/game.ts` (the `Main` durable object), `wrangler.jsonc`, `party/tsconfig.json`; `src/lib/party/room-state.ts` (the pure, unit-tested orchestration logic — 16 tests), `room-token.ts`, `protocol.ts`, `finalize-match.ts`, `achievements.ts`, `public-rooms.ts`, `use-party-room.ts`; `src/app/api/identity/route.ts`, `src/app/api/public-rooms/route.ts`. Schema simplified 11 → 8 tables. **Not yet verified against a real deployment.** See `DECISIONS.md` D-012/D-013/D-014/D-015.
  4. **`/profile`/`/leaderboard` onto Neon reads** — ✅ Server Components reading Neon via Drizzle, resolving identity via `getCurrentActor()`; new `PATCH /api/profile` route. See `DECISIONS.md` D-016.
  5. **Full Supabase removal** — ✅ `src/lib/supabase/*`, the `@supabase/*` deps, `supabase/` (migrations + seed), the Supabase bootstrap block in `middleware.ts`, the pre-PartyKit `lib/multiplayer/*` orchestration files + 7 dead API routes + `lib/achievements/evaluate.ts`, all deleted; `CLAUDE.md`/`ARCHITECTURE.md`/`DATABASE.md`/`API_REFERENCE.md`/`SECURITY.md`/`DEPLOYMENT.md`/`FEATURES.md`/`README.md`/`TESTING.md`/`FILE_MAP.md`/`ROADMAP.md` rewritten to describe the new backend as the only backend. See `DECISIONS.md` D-017.
- **What has been completed:** all 5 phases, in full. `npm run typecheck && npm run typecheck:party && npm run lint && npm run test && rm -rf .next && npm run build && npx wrangler deploy --dry-run` all pass. **Zero Supabase code, dependency, or documentation reference remains anywhere in the repository.**
- **What remains:** nothing under T-003 itself — see "High priority" below for the next real work (all of it live-verification/dashboard-config, not migration).
- **Relevant files:** `src/lib/db/*`, `src/lib/identity/*`, `src/middleware.ts`, `src/app/layout.tsx`, `src/components/profile/account-linking.tsx`, `party/*`, `src/lib/party/*`, `src/lib/multiplayer/{room-code,rate-limit,extract-score,types}.ts` (the 4 surviving backend-agnostic utilities), every game board.
- **Known errors:** none in what's built.
- **Blockers:** the user needs to create a **Neon project** (`DATABASE_URL`), a **Clerk application** (keys), and a **Cloudflare account** (to actually deploy `party/game.ts`) — all account-level actions outside this agent's ability to perform. None block writing more code — there's nothing left to write for the migration itself.
- **Acceptance criteria (met):** the app's code runs the full core loop (create room → join → play a match → see it on the leaderboard/profile) with zero Supabase involvement, using Clerk + Neon + PartyKit exclusively; all 10 games still function identically online (unchanged by the migration); `typecheck`/`typecheck:party`/`lint`/`test`/`build`/`wrangler deploy --dry-run` all pass; every memory doc accurately describes the new architecture with no Supabase-era doc left uncorrected. **The one criterion still open is live verification against real credentials — tracked separately below, not part of T-003's own scope.**

**T-004 — full detail, for historical record:**

- **Exact objective:** replace PartyKit on Cloudflare Workers (Durable Objects) with Ably pub/sub as the realtime transport, since Cloudflare's Durable Objects require the $5/month Workers Paid plan — real money for what's a casual GamePigeon-style friends game — while keeping Clerk/Neon/Drizzle/Vercel and every game's engine/board/bot logic completely unchanged.
- **What was done:** added the `live_rooms` Neon table (one JSONB row per active room, holding the same `StoredRoomState` shape a Durable Object used to hold in memory, since Ably itself has no compute/storage); moved `src/lib/party/` → `src/lib/realtime/`, renaming `Party*` types to `Live*`/`Room*`; replaced `party/game.ts`'s WebSocket `onMessage` switch with a stateless `POST /api/rooms/[code]/action` Route Handler; added `GET /api/rooms/[code]` (the initial-state fetch a Durable Object's `onConnect` used to provide for free); replaced the HMAC room-token scheme with `GET /api/ably-token` minting a real Ably `TokenRequest`; rewrote the client hook as `use-realtime-room.ts`; worked around a real Next.js/webpack incompatibility with Ably's bundled npm output (an unparseable `super(...args)` inside a constructor's arrow function) by loading the Ably client SDK from a CDN `<script>` tag instead of an npm import; re-added `navigator.sendBeacon`-based disconnect detection and a Vercel Cron idle-room sweep (both acknowledged regressions from what a Durable Object gave for free — see `DECISIONS.md` D-018). Deleted `party/`, `wrangler.jsonc`, `.wrangler/`, `worker-configuration.d.ts`, `src/lib/party/room-token.ts`, `src/app/api/identity/`; removed `partyserver`/`partysocket`/`wrangler`/`concurrently` deps and their npm scripts; added `ably`. Rewrote every memory doc (`CLAUDE.md`, `README.md`, `ARCHITECTURE.md`, `DATABASE.md`, `API_REFERENCE.md`, `SECURITY.md`, `DEPLOYMENT.md`, `TESTING.md`, `FEATURES.md`, `FILE_MAP.md`, `ROADMAP.md`).
- **What has been completed:** all of it. `npm run typecheck && npm run lint && npm run test && rm -rf .next && npm run build` all pass (14 routes, 169 tests).
- **What remains:** nothing under T-004 itself — live verification against a real Ably app is part of the same "High priority" live-verification item as Neon/Clerk, not a separate task.
- **Relevant files:** `src/lib/realtime/*`, `src/app/api/rooms/[code]/*`, `src/app/api/ably-token/route.ts`, `src/app/api/cron/cleanup/route.ts`, `src/lib/db/schema.ts` (`liveRooms`), `src/components/room/room-page-client.tsx` (the Ably CDN script tag), every online-game component that imports the realtime hook.
- **Known errors:** none in what's built.
- **Blockers:** the user needs to create an **Ably account/app** (`ABLY_API_KEY`) — account-level, outside this agent's ability to perform. Nothing left to write.
- **Acceptance criteria (met):** all 10 games' online play works identically to before the rework (unit tests unchanged in substance, only renamed); zero PartyKit/Cloudflare/wrangler code, dependency, or doc reference remains anywhere in the repository; the honest regressions (rate-limiter scoping, presence heuristic) are documented in `SECURITY.md`/`DECISIONS.md`, not hidden.

### T-001 (superseded, kept for history — do not act on this)

Original text: "Connect a real Supabase project and run the manual smoke-test checklist." Superseded in full by T-003 above the moment the user said Supabase was not an option. Left here rather than deleted so a future reader doesn't wonder whether it's still relevant — it is not.

### T-002 (superseded, kept for history — do not act on this)

Original text: "Run `npm run test:e2e` against the newly-live backend and fix selector/flow drift" (blocked on T-001). Superseded along with T-001 — the two Playwright specs will need rewriting once Phase 3 lands, since the app they drive will no longer boot against Supabase at all. Tracked fresh under T-003's phase 3/verification steps instead.

## Blocked

Nothing left is blocked on missing credentials or infrastructure — Neon, Clerk, Ably, and a live Vercel deployment all exist as of this session (see `PROJECT_STATE.md`). What's left needs a human browser, not another account.

## High priority

- **T-005: real two-browser manual click-through** (`TESTING.md`'s smoke-test checklist, against https://pocket-party-eta.vercel.app) and **`npm run test:e2e`** — now actually runnable for the first time. Neither requires the user to create or configure anything further; both just need someone to sit at a browser (or run Playwright) and watch what happens. This is the last thing standing between "wiring confirmed correct via curl" and "actually known to work."
- `CRON_SECRET` is now set (generated and pushed to Vercel this session) and Google-OAuth-dashboard configuration is still open — a social sign-in provider (if offered) is configured in the Clerk dashboard, not a Supabase one.
- Connect the existing GitHub repo (`Gariyuuu/pocket-party`) to the Vercel project for auto-deploy-on-push — the repo/remote now exist (see `PROJECT_STATE.md`), just not yet linked in Vercel, so every deploy so far has still been a manual `vercel deploy --prod` from the local directory.

## Medium priority

- **Reconsider the rate limiter's storage** — a real, honest regression from the Ably rework: it's now scoped per-Vercel-serverless-function-instance, not per-room the way it was when it lived inside a long-lived Cloudflare Durable Object (see `DECISIONS.md` D-018, `SECURITY.md`). A Neon-backed or Redis-backed counter would restore a shared, reliable limit if this ever needs to scale past one friends-game's traffic.
- **Add rate limiting to `GET /api/ably-token`** — currently unthrottled beyond Clerk/Vercel defaults, and every room-page load calls it (`SECURITY.md`'s recommended fix 5).
- Wire `src/lib/log.ts`'s `logError()` (the structured choke point added earlier — see `SESSION_LOG.md`) to a real APM/monitoring service (e.g. Sentry) before real users hit production; today its output only reaches `console.error`.
- ~~Consider a UX fallback for the Ably-CDN-unreachable case~~ — **done 2026-08-15** (`09c0ede`): `useRealtimeRoom` now has an explicit error state + `retry()`, surfaced as a "Try Again" button. Not yet confirmed via an actual simulated-outage click-through — that verification is still open.
- **Add Playwright e2e specs for the 12 games added after the original suite** (Mini Golf, Word Bites, Sea Battle, Checkers, Chess, Darts, Cornhole, Reversi, Dots and Boxes, Yahtzee, Mancala, Trivia Blitz) — the only 12 of 22 games without one. See `TESTING.md`'s coverage gap 7 for the intended approach for each.
- **Have a human actually listen to the new ambient music** — `tests/unit/music.test.ts` verifies the generated audio is non-silent/non-clipping/click-free at the loop edges, but audio *quality* is inherently not something static checks can confirm.
- **Write real `patch-notes.ts` entries (and matching `CHANGELOG.md` entries) for everything since 0.7.0** — the in-app `/patch-notes` page and `CHANGELOG.md` both stop at "0.7.0 — Polish pass," but 12 games, a canvas-color bugfix, an ambient-music rewrite, a cron fix, and the first live deployment have all shipped since. Deliberately left undone in the 2026-08-07 documentation checkpoint pass (product-content authoring — real version numbers and user-facing copy — not a documentation-accuracy fix). See `CHANGELOG.md`'s trailing note for detail.

## Low priority

None currently open — the two unused deps and the scaffold SVGs (below) were the only items here, both done.

## Bugs

None currently known/reproducible (nothing is running to reproduce a bug against). The closest thing to a "known issue" is the set of never-tested paths listed in `PROJECT_STATE.md`'s "Blockers" — these are unverified, not confirmed-broken.

## Technical debt

See `FILE_MAP.md`'s "Technical debt" section for the full list — duplicated here only by reference to avoid two sources of truth drifting apart.

## Testing needed

`src/lib/realtime/room-state.ts` has direct unit coverage (16 tests, `tests/unit/realtime-room-state.test.ts`). Still needed: e2e coverage actually run (all specs exist and parse, none have executed), e2e specs for Mini Golf and Word Bites specifically (the only 2 of 12 games without one — both have full unit coverage), direct tests for `POST /api/rooms/[code]/action`'s I/O shell and `src/lib/realtime/achievements.ts`/`finalize-match.ts` (currently only typechecked, needs either a real Neon connection or a mocked `getDb()` to test against), and a real two-browser click-through of the whole Ably stack now that credentials/deployment both exist.

## Documentation needed

None currently — every memory doc was rewritten, first during Phase 5 to describe the Clerk + Neon/Drizzle + PartyKit stack as the only stack, then again during T-004 to replace every remaining PartyKit/Cloudflare reference with Ably (`CLAUDE.md`, `ARCHITECTURE.md`, `DATABASE.md`, `API_REFERENCE.md`, `SECURITY.md`, `DEPLOYMENT.md`, `FEATURES.md`, `README.md`, `TESTING.md`, `FILE_MAP.md`, `ROADMAP.md` all rewritten; see `DECISIONS.md` D-017/D-018), then again to add Mini Golf and Word Bites to every game-count/game-list reference across those same files, then again to add Sea Battle/Checkers/Chess/Darts/Cornhole the same way (12 → 17 games throughout), then again to add Reversi/Dots and Boxes/Yahtzee/Mancala/Trivia Blitz plus the music rewrite (17 → 22 games throughout). Future gaps should be added here as they're found.

## Recently completed

- **Documentation onboarding pass (2026-08-17)** — found that every memory file's "current state" claims had stopped at commit `1f88738` (2026-08-07) despite 4 real commits landing since with no doc updates. Corrected `PROJECT_STATE.md`, `CLAUDE.md`, and this file to describe the actual current state, including fixing a "known issue" (`CLAUDE.md` #5, Ably-CDN-unreachable UX) that had already been resolved in code 2 days after the doc claiming it was still open. No product code changed.
- **Added a connection-error state + retry UI to the room-join flow** (`09c0ede` 2026-08-15, merged via `chore/polish` at `a622ee2` 2026-08-16) — `useRealtimeRoom` gained an explicit `"error"` status and `retry()`; `room-page-client.tsx` shows a "Try Again" button plus a `thinking-orbs` connecting animation instead of hanging silently on a stuck Ably connection.
- **Added OpenGraph/Twitter metadata, `robots.ts`, `sitemap.ts`** (`32c8613`, 2026-08-13) — site-level and per-game dynamic `opengraph-image.tsx`, expanded `layout.tsx` metadata, `robots.ts` and `sitemap.ts` covering static routes and every game page.
- **Added ARIA live regions/labels across game shell, lobby, scoreboard** (`6d5bdde`, 2026-08-13) — `aria-live` status announcements for match results/round progress, `aria-pressed`/`aria-describedby` on ready/start buttons, `role="region"` + labeled live regions for the lobby player list and scoreboard.
- **Added a `safeQuery` fallback helper for read-path DB errors** (`2bb3491`, 2026-08-11) — wraps the reads behind `/leaderboard`, `/profile`, and `GET /api/public-rooms` in `src/lib/db/client.ts` so a Neon hiccup degrades to an empty result instead of crashing the page. Deliberately not applied to identity-claim logic or any live-game write path (those should fail loudly, not silently).
- **Rewrote the ambient music, fixed a real solo-bot bug, and added 5 more games: Reversi, Dots and Boxes, Yahtzee, Mancala, and Trivia Blitz** (2026-08-07) — user request ("the music sucks make something else and add 5 more games"). **Music:** the old loop was a single repeating 4-note sine arpeggio; replaced with a real 8-bar vi-IV-I-V chord progression (Am-F-C-G) built from 3 overlapping voices (bassline, sustained chord pad, plucked melody that reverses direction on the second pass). Required extending `tone.ts` with a genuinely polyphonic mixer (`buildMixedToneDataUri`/`NoteEvent`, per-note ADSR envelopes, additive mixing) alongside the existing single-voice `buildToneDataUri` (left untouched, still used by every SFX). New `tests/unit/music.test.ts` decodes the actual generated WAV and checks it's non-silent, non-clipping, and fades to near-zero at both loop edges. **A real bug found and fixed along the way:** designing Yahtzee's bot exposed that the generic solo-mode bot dispatch in `solo-game-shell.tsx` only ever called `getBotAction` once per turn handoff — fine for single-action turns, but Darts/Cornhole need 3-4 actions per turn and confirmed via a live Playwright run to get **permanently stuck** after the bot's first throw (dart 1/3, forever). Fixed by replacing the one-shot call with a recursive `runBotTurn` loop that keeps acting until the turn actually passes; re-verified Darts' bot now completes all 3 throws, and this fix is also what makes Mancala/Dots and Boxes/Yahtzee's bonus-turn rules work correctly for their bots. **5 new games**, chosen to avoid mechanical overlap with the existing 17: Reversi (disc-flipping, auto-skips a player with zero legal moves instead of requiring an explicit pass), Dots and Boxes (edge-claiming, extra turn on completing a box, hard-difficulty bot simulates the resulting chain reaction when forced to sacrifice), Yahtzee (3 rolls/turn, 13 categories, standard upper-section bonus, deliberately no "multiple Yahtzee" joker bonus), Mancala (classic Kalah rules, sowing/capture/bonus-turn), Trivia Blitz (simultaneous multiple-choice answering, no turn order, no timer, hand-wired bot like Word Clash). All wired into `registry.ts`/`engines.ts`/`game-surface.tsx`/`game-content.ts`/`game-card.tsx`'s icon map/`solo-game-shell.tsx`. Added 62 new unit tests (320 total, up from 258) — one carefully hand-verified auto-skip scenario for Reversi, a double-box-completion test for Dots and Boxes, a bounded-loop full-turn simulation for the Yahtzee bot, a seed-conservation check for Mancala, and a full-match simulation for Trivia Blitz. Full verification suite passes. **Visually verified via real Playwright screenshots and interaction scripts** — disc flips and the bot's response for Reversi, line claims for Dots and Boxes, live-updating scoring previews and a working reroll flow for Yahtzee, correct seed distribution for Mancala, and a complete 8-round Trivia Blitz match played out via script (including catching and fixing a second real bug: the bot's answer effect used a round-scoped ref guard that got permanently blocked once the human's own click cancelled its in-flight timeout — removed the redundant guard, now relies solely on `answers[BOT_ID]`). **Redeployed successfully on the first attempt** (`vercel deploy --prod --yes`, no quota block) — confirmed all 5 new game pages return `200` against the live production URL. **The live site now serves all 22 games.**
- **Added 5 new games: Sea Battle, Checkers, Chess, Darts, and Cornhole; redeployed** (2026-08-07) — user request ("ok 5 more cool games that are not added from game piedgoen"), chosen specifically to avoid mechanical overlap with the existing 12 (billiards/basketball/cup-pong/tank-artillery/air-hockey already covered). Sea Battle: classic Battleship on an 8x8 grid, a non-turn-based fleet-placement phase (`fleets: Record<playerId, Ship[]|null>`, transitions to `"battling"` once both are set) handled by a dedicated `useEffect` rather than the generic bot-dispatch mechanism, hunt-heuristic bot. Checkers: mandatory-capture enforcement, optional (not forced) multi-jump chain continuation as a documented simplification, kings. Chess: full rules including check/checkmate/stalemate detection, castling (with attacked-square checks), en passant, promotion, castling rights recomputed fresh after every move rather than tracked incrementally (handles "rook was captured" for free), board flips per viewer, material-eval + 2-ply-minimax bot. Darts and Cornhole: near-identical turn/round structure, both reusing `games/core/physics.ts`'s `simulateProjectile` with different target-plane models (vertical ring board vs. horizontal platform-with-a-hole), grid-search bots. All 5 wired into `registry.ts`/`engines.ts`/`game-surface.tsx`/`game-content.ts`/`game-card.tsx`'s icon map/`solo-game-shell.tsx`. Added 54 new unit tests (258 total, up from 204) — caught and fixed one real test-authoring bug along the way: an initial hand-constructed "checkmate" position for Chess wasn't actually checkmate (a pawn could capture the checking knight diagonally), rewritten as a proper smothered-mate-style position and re-verified passing; the engine code itself was correct throughout. Full verification suite (`typecheck`/`lint`/`test`/`build`) passes. **Visually verified via real Playwright screenshots and interaction scripts, not just static review** — placement/randomize-and-battle for Sea Battle, piece selection/legal-move-highlighting for Checkers, a full pawn-move → bot-response cycle for Chess (confirmed via DOM `aria-label` inspection that the "filled-looking" king/queen glyphs in a screenshot were a headless-Chromium font-rendering quirk, not a logic bug — the underlying Unicode codepoints and piece assignments were verified correct), a dart landing in the bullseye, a cornhole bag landing on the board. Along the way, confirmed a pre-existing hydration-mismatch console warning (the game-header's "Toggle fullscreen"/"Rules" button order swapping between SSR and client) reproduces on old games too (e.g. Grid Three) — not introduced by this session, not fixed, flagged in `CLAUDE.md`'s Known Issues. **Redeployed successfully on the first attempt** (`vercel deploy --prod --yes`, no quota block this time) — confirmed all 5 new game pages return `200` against the live production URL. **The live site now serves all 17 games.**
- **Swept the canvas-color bug and a real off-canvas-rendering bug across the other 5 physics games; redeployed** (2026-08-07) — follow-up to the Mini Golf/Word Bites fix below: user asked to fix the remaining minigames' UI/UX too. Confirmed via `grep` that Bounce Cup, Mini Hoops, Tank Tactics, Pocket Shots, and Orb Hockey all had the same `var(--color-party-*)`-in-canvas bug as Mini Golf (Quick Draw didn't — it only uses `currentColor`, which canvas *does* support). Added a shared `resolveCssVar(s)` helper (`games/core/canvas-color.ts`) and applied it across all 5 boards, plus colorful backgrounds/felt/court tints, brighter cup/hoop/goal/pocket markers, and white outline rings on every ball/paddle/tank for contrast. **Found a second, independent, real bug along the way** — Bounce Cup and Mini Hoops each drew their ground line/cups/hoop at a y-coordinate (`TABLE_GROUND_Y=320`, `GROUND_Y=360`) below their own canvas's pixel height (`CANVAS_HEIGHT=220`), so those elements had been rendering completely off-canvas since these games were first built — confirmed via a before/after Playwright screenshot showing an empty tinted rectangle with no shooter, cups, or hoop visible at all. Fixed by raising `CANVAS_HEIGHT` to actually contain the ground in each (this constant is display-only, not read by the physics engine, so the fix carries zero gameplay risk). Verified all 5 games visually via Playwright screenshots before and after, not just by reading the code. Full verification suite passes. Redeployed successfully — the live site now reflects every fix from this session.
- **Fixed real bugs in Mini Golf and Word Bites** (2026-08-07) — user feedback: Word Bites' rack wrapped across multiple centered rows, breaking the left/right adjacency the tap-to-extend mechanic depends on (fixed: single non-wrapping horizontally-scrollable row, plus color-coded tiles by source-word group and a highlight on whichever tile(s) would currently extend the selection). Mini Golf: found and fixed a **real, confirmed-via-screenshot bug** — every canvas `fillStyle`/`strokeStyle` referencing `var(--color-party-*)`/`var(--color-player-*)` was silently failing to resolve (Canvas 2D's color parser doesn't support CSS custom properties at all, only literal color syntax), so the course rendered as flat gray instead of the intended colorful stripes/obstacles — this is almost certainly the same root cause behind the user's broader "the games are too bland" impression, though only Mini Golf/Word Bites were in scope to fix this pass. Fixed by resolving each CSS variable to its computed literal value via `getComputedStyle` once per draw (still theme/dark-mode-reactive) instead of passing the unresolved `var()` string. Added a striped fairway, a flag+pin marker on the hole, and a dashed ball-to-hole guide line shown on your turn. Verified visually via Playwright screenshots (not just curl) before and after the fix — confirmed the gray-canvas bug and confirmed the fix resolves it. Full verification suite passes. **Redeploy attempted immediately after, blocked again**: `"Resource is limited - try again in 1 day"` — the same account-wide Hobby-plan daily cap, apparently already re-exhausted by other projects on the account today. The fix is committed to the working directory and fully tested; only the redeploy itself is pending.
- **Redeployed with Mini Golf and Word Bites live** (2026-08-07) — the previous session's redeploy had been blocked by Vercel's Hobby-plan daily deployment cap; retried `vercel deploy --prod` once the user confirmed the quota had reset, succeeded on the first attempt. Confirmed via `curl` against the live URL: `/game/mini-golf` and `/game/word-bites` both `200`, and a full room-action round-trip (`join` → `select-game` with `gameId: "mini-golf"` → `leave`) against production, confirming the new `GameId` is recognized end to end by the deployed server, not just locally. **The live site now serves all 12 games.**
- **Added two new games: Mini Golf and Word Bites** (2026-08-06) — user request ("add minigolf if u havnt and word bites"). Mini Golf: 3 fixed obstacle courses, per-player-ball turn rotation (skipping anyone already holed out), a new single-ball rolling-friction physics model (`games/mini-golf/physics.ts` — different from the shared arc-shot `games/core/physics.ts`, since a putt rolls rather than flies), a per-hole stroke cap to force resolution, grid-search bot. Word Bites: a shared, shrinking rack of 1-3 letter "bites" built from a small curated word list (`games/word-bites/seed-words.ts`) and shuffled by source word (keeping each word's bites contiguous), claim a connected in-order run that's a real word to score `length²` points, real-word validation reuses the same big dictionary Word Clash uses. Refactored `games/word-clash/dictionary.ts` → `games/core/dictionary.ts` (+ `word-list.json`) since it's now genuinely shared by two games, not one. Both new games wired into `registry.ts`/`engines.ts`/`game-surface.tsx`/`game-content.ts`/`game-card.tsx`'s icon map; Mini Golf also added to `physics-heavy-games.ts` (landscape hint). Added 32 new unit tests (physics, engine, board-gen, bot for both games) — 204 total now, up from 172. Full verification suite (`typecheck`/`lint`/`test`/`build`) passes; confirmed both games' solo-mode pages boot cleanly via a local dev-server `curl` check (not a full browser click-through). Attempted a same-day redeploy, which hit Vercel's Hobby-plan daily deployment cap — succeeded the next day once the quota reset (see the entry below). Every game-count reference across the memory docs updated from 10 to 12 (`CLAUDE.md`, `README.md`, `FEATURES.md`, `FILE_MAP.md`, `TESTING.md`, `ROADMAP.md`, `HANDOFF.md`). **Known gap, honestly flagged, not hidden:** neither game has a Playwright e2e spec yet (the only 2 of 12 without one), and neither has been clicked through in a real browser — see `TESTING.md` coverage gap 7.
- **Deployed to Vercel for the first time** (2026-08-06) — added `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`/`CLERK_SECRET_KEY`/`ABLY_API_KEY` (via `vercel env add`) and a newly-generated `CRON_SECRET` to the `pocket-party` Vercel project across Production/Preview/Development. First `vercel deploy --prod` **failed**: the Hobby plan rejected `vercel.json`'s hourly cron schedule outright ("Hobby accounts are limited to daily cron jobs"). Loosened to `"0 3 * * *"` (daily) and redeployed — succeeded. Live at https://pocket-party-eta.vercel.app; no deployment-protection/SSO wall was blocking it (confirmed via an unauthenticated `curl`, unlike some of this developer's other projects). Re-ran the same guest-identity/room-join/Ably-token `curl` smoke test from the previous session against the live production URL instead of localhost — all passed identically. Docs updated: `DEPLOYMENT.md` (full rewrite — no longer describes an "intended" process, describes what's actually live), `ARCHITECTURE.md`/`API_REFERENCE.md`/`README.md`/`TESTING.md`/`FILE_MAP.md`/`CLAUDE.md` (hourly→daily cron references corrected), `CLAUDE.md`/`README.md` (production-status framing updated from "not deployed" to live), `TASKS.md`/`PROJECT_STATE.md`/`HANDOFF.md`. Still not done: a real browser click-through and `npm run test:e2e` against the live URL, and connecting a git repo for auto-deploy-on-push.
- **Live infrastructure provisioned; app exercised against it for the first time** (2026-08-06) — provisioned a real Neon database via Vercel's Marketplace integration (`vercel install neon`), created and linked a real Vercel project (`pocket-party`), connected the database, pulled `DATABASE_URL` locally via `vercel env pull`. Received real Clerk keys and an Ably API key from the user directly, wrote them to `.env.local` (confirmed gitignored). Ran `npm run db:migrate` against live Neon (succeeded, 9 tables). Ran `npm run db:seed` — **failed the first time** on a real, previously-latent bug: `src/lib/db/seed.ts` imported `getDb()` from `client.ts`, whose `import "server-only"` throws unconditionally outside a bundler that aliases it away (i.e. always, under plain `tsx`) — this script could never have worked since the moment that import was added, just never noticed because no `DATABASE_URL` existed to get that far before. **Fixed** by having `seed.ts` build its own `drizzle(neon(getDatabaseUrl()))` client instead of importing `client.ts`; `client.ts`'s `server-only` guard is untouched. Reseeded successfully (5 achievements). Booted `npm run dev` and confirmed via `curl`: guest identity provisioning, `POST /api/rooms/[code]/action` (join + leave), `GET /api/rooms/[code]`, `GET /api/ably-token` (a real token minted with correct per-room capability scoping), `/leaderboard`, `/profile`, `/room/[code]` — all `200`, no errors. Full verification suite (`typecheck`/`lint`/`test`/`build`) re-confirmed passing after the fix. See `PROJECT_STATE.md` for the full account, including what's explicitly still *not* verified (a real two-browser session, `test:e2e`, and any actual deployment).
- **T-004: PartyKit/Cloudflare → Ably realtime rework** (2026-08-06) — see the T-004 section above and `DECISIONS.md` D-018 for the full reasoning and technical writeup (including the Next.js/Ably bundler-incompatibility investigation). Full verification suite passing (typecheck/lint/169 tests/clean-cache build, 14 routes). Every memory doc rewritten.
- **Removed the 2 unused deps and the 5 scaffold SVGs** (2026-08-06) — `nanoid`/`@testing-library/react` dropped from `package.json` (confirmed zero imports first), `public/{file,globe,next,vercel,window}.svg` deleted (confirmed zero references first). Full verification suite passing.
- **Added e2e coverage for the other 9 games' online flow** (2026-08-06) — new `tests/e2e/helpers.ts` (shared room-setup boilerplate, also used to de-duplicate the 2 existing specs), plus one spec per remaining game: `fourfall`, `bounce-cup`, `mini-hoops`, `tank-tactics` (all: click the always-accepted shoot/fire/drop action, confirm turn passes to the guest), `pocket-shots` (a deliberately weak drag-based shot engineered to be a guaranteed no-contact foul, since the rack sits 300+ world-units from the cue ball's spawn), `quick-draw` (detects which player is the artist from the rendered UI rather than assuming seat order, has the guesser submit an answer), `tile-rush` (reads every tile's `aria-label`, which already encodes color+position, to find a real adjacent same-color pair at runtime instead of guessing one), `word-clash` (render-only — a seeded, unknown-in-advance letter pool makes scripting a real dictionary word blindly infeasible), `orb-hockey` (render-only plus the automatic countdown-to-live transition — real-time paddle/puck physics has no single scriptable "score a goal" action). All 13 tests across 11 files confirmed to parse/list correctly (`npx playwright test --list`) — still blocked on real credentials to actually execute.
- **Added structured error logging** (2026-08-06) — new `src/lib/log.ts`'s `logError(context, error, meta?)`, an isomorphic (Next.js server / browser / Cloudflare Worker) choke point for reporting unexpected errors as structured JSON. Wired into `party/game.ts`'s 3 existing best-effort failure paths, added try/catch + logging to all 3 Next.js API routes (which previously had none), and `src/app/error.tsx`'s boundary (`global-error.tsx` deliberately left alone — it's intentionally dependency-free). Fixed a real bug found along the way: `public-rooms-list.tsx`'s fetch didn't check `res.ok`, so the new error-shaped 500 response would have crashed it (`undefined.length`) — added an `res.ok` check. 3 new unit tests for `logError` (172 total now). Full verification suite passing. Docs updated: `SECURITY.md`, `ARCHITECTURE.md`, `TASKS.md`, `PROJECT_STATE.md`.
- **Rate-limited the PartyKit `"join"` message** (2026-08-06) — `party/game.ts`'s `"join"` case now checks `checkRateLimit(join:<room>:<profileId>, 5, 10_000)` before calling `joinRoom()`, matching the existing pattern used for `"action"`. Closes the gap `SECURITY.md` previously flagged, with an honestly-scoped limitation documented there: it stops same-room join spam, not an attacker guessing many different room codes (each is a separate Durable Object with independent limiter state — closing that would need edge/shared-store rate limiting, not implemented). Docs updated: `SECURITY.md`, `CLAUDE.md`'s Known Issues.
- **Rewrote both Playwright e2e specs for the PartyKit flow** (2026-08-06) — `tests/e2e/room-join.spec.ts` and `grid-three.spec.ts` updated for the current identity-dialog-before-navigation flow, the `?autojoin=1` query param, and a first-time tutorial dialog that needs dismissing. The old "invalid room code" test was replaced with a reconnect-into-the-same-seat test, since there's no invalid-code case anymore (rooms are created implicitly on first join). `playwright.config.ts`'s `webServer` now starts both `npm run dev` and `npm run dev:party`. Confirmed both specs parse/list correctly (`npx playwright test --list`, 4 tests/2 files) — still blocked on real credentials to actually execute.
- **Migration Phase 5: full Supabase removal** (2026-08-06) — see T-003 above and `DECISIONS.md` D-017. Deleted `src/lib/supabase/*`, 9 pre-PartyKit `lib/multiplayer/*` files, `src/lib/achievements/evaluate.ts`, 2 already-orphaned dead files (`use-current-user-id.ts`, `profile-card.tsx`), 7 dead API routes, `supabase/`, `vercel.json`; removed the `@supabase/*` deps and the `db:types` script; removed the Supabase bootstrap block from `middleware.ts`; rewrote every memory doc. Full verification suite passing (13 routes, down from 20). **The backend migration (T-003) is now fully complete.**
- **Migration Phase 4: `/profile`/`/leaderboard` onto Neon reads** (2026-08-06) — see T-003 above and `DECISIONS.md` D-016. `profile/page.tsx`, `leaderboard/page.tsx`, `profile-header.tsx` rewritten; new `PATCH /api/profile` route. Full verification suite (typecheck/typecheck:party/lint/169 tests/clean-cache build/wrangler dry-run) passing — zero Supabase reads or writes remain in the live code path.
- **Phase 3 Follow-up B: Neon-backed public-rooms directory** (2026-08-06) — see T-003 above. `/lobby`'s public-rooms browser now works on the new stack. See `SESSION_LOG.md`'s latest entry and `DECISIONS.md` D-015.
- **Phase 3 Follow-up A: Orb Hockey + Quick Draw broadcast sync** (2026-08-06) — see T-003 above. All 10 games now have full online play on the new stack. Full verification suite (including a clean-cache `npm run build` and `wrangler deploy --dry-run`) passing. See `SESSION_LOG.md`'s latest entry and `DECISIONS.md` D-014.
- **Migration Phase 3: PartyKit room/match engine** (2026-08-06) — see T-003 above for the full file list. 16 new unit tests. See `SESSION_LOG.md`'s Phase 3 entry and `DECISIONS.md` D-012/D-013.
- **Migration Phase 2: Clerk + guest-cookie identity layer** (2026-08-06) — see T-003 above for the full file list. `middleware.ts`/`layout.tsx`/`account-linking.tsx` edited (not just added-to), verified non-breaking. See `SESSION_LOG.md`'s latest entry and `DECISIONS.md` D-011.
- **Migration Phase 1: Neon + Drizzle schema** (2026-08-06) — see T-003 above for the full file list. Purely additive; nothing existing was touched; full verification suite re-confirmed passing. See `SESSION_LOG.md`'s latest entry.
- **Account-switch checkpoint** (2026-08-06) — re-verified git state, discovered the stray Supabase-link file (see `PROJECT_STATE.md`), re-ran all 4 verification commands (still passing), confirmed no secrets in any doc, confirmed task consistency across `CLAUDE.md`/`PROJECT_STATE.md`/`TASKS.md`/`HANDOFF.md`, and folded several conversation-only decisions (theme-wheel design choices) into `DECISIONS.md`/`UI_SYSTEM.md`. No product code changed. See `SESSION_LOG.md`'s latest entry.
- **The documentation/handoff audit** (17 memory files created, no product code changed) — see `CHANGELOG.md` and `SESSION_LOG.md`.
- All 10 games (engines, boards, bots, tests) — prior session, before this audit.
- Multiplayer room/match infrastructure — prior session.
- Accounts/progression (profiles, achievements, leaderboard, recent opponents, account linking) — prior session.
- App icon, `/patch-notes` page, theme wheel (3 palettes × light/dark procedural backgrounds) — prior session, most recent product work before this audit.

## Deferred

Nothing explicitly deferred by the user — the "Post-MVP" list in `ROADMAP.md` is candidate future work, not yet prioritized or committed to.

## Rejected ideas

None recorded anywhere in the repository. Not fabricated here.
